<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Volt dark background #111316 -->
    <item android:drawable="@color/volt_background" />
    <!-- Centered bolt accent – uses mipmap/launcher as placeholder, tinted via foreground if added -->
    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/ic_launcher" />
    </item>
</layer-list>
